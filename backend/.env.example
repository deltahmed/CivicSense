# Copier ce fichier en .env et remplir les valeurs

SECRET_KEY=remplace-par-une-cle-secrete-longue-et-aleatoire
DEBUG=True

# PostgreSQL
DB_NAME=civicsense
DB_USER=postgres
DB_PASSWORD=ton_mot_de_passe_postgres
DB_HOST=localhost
DB_PORT=5432

# Email (Mailtrap en dev — récupérer les credentials sur mailtrap.io)
EMAIL_HOST=sandbox.smtp.mailtrap.io
EMAIL_PORT=2525
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=True
DEFAULT_FROM_EMAIL=noreply@civicsense.local

# JWT (laisser vide = utilise SECRET_KEY)
JWT_SIGNING_KEY=

# CORS
CORS_ORIGIN=http://localhost:5173
